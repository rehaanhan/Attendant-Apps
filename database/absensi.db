CREATE TABLE faces (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    nama TEXT NOT NULL,
    encoding TEXT NOT NULL
);

CREATE TABLE absensi (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    nama TEXT,
    waktu TEXT,
    mood TEXT
);
